# LTX 2.5 I2V motion prompt

Use the provided image as the exact first frame. Continue naturally from what is already visible; do not redesign the characters or scene.

Action and timing: `{story_action}`
Camera movement: `{camera}`
Duration: `{duration_s}` seconds.
Audio: `{audio_direction}`

Keep character identity, fur pattern, wardrobe, scale, lighting direction, background geometry, and object placement stable. Motion should have clear anticipation, one readable main action, and a settled ending. Avoid morphing, duplicated limbs, sliding feet, camera jumps, flicker, background warping, added characters, subtitles, and logos.
